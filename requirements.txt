# Vantage dependencies
# Runs with zero external services (SQLite) and zero API keys (heuristic AI fallback).
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
jinja2==3.1.4
python-multipart==0.0.9
httpx==0.27.2
feedparser==6.0.11
# Optional: only used if VANTAGE_LLM_PROVIDER is set to a live provider.
# openai==1.51.0
